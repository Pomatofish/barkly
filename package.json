{
  "name": "grammy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Marker so Node treats .js as ES modules for tests. Chrome ignores this file. No dependencies.",
  "scripts": {
    "serve": "node tools/serve.js",
    "test": "node --test"
  }
}
