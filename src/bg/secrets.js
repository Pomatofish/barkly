// src/bg/secrets.js — built-in API key. Paste your OpenAI key between the quotes.
// This file is marked skip-worktree in git so your local edit is never committed;
// the repo copy stays empty. A key saved on the onboarding page always wins over this one.
export const DEFAULT_API_KEY = '';
